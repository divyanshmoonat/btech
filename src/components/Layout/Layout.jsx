import { Outlet } from "react-router-dom";

import Header from "../Header/Header";

const Layout = () => {
  return (
    <>
      {/* Header */}
      {/* Content */}
      {/* Footer */}

      <Header />
      <Outlet />
    </>
  );
};

export default Layout;
